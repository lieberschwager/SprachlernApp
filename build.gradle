buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.1'
    }
}

allprojects {
    tasks.withType(JavaCompile).configureEach {
        options.release = 17
    }
}
